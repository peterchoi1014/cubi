//! Isolated subprocess subagent runner (slice A — under construction).
//!
//! Spawns the `cubi` binary headlessly inside a caller-supplied working
//! directory so a subagent's tool actions are confined to that directory.
